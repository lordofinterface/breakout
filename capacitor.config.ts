import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.game12456',
  appName: 'Test',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
